plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin" // Flutter plugin
}

android {
    namespace = "com.example.pet_care_finder"
    compileSdk = 34 // Align with targetSdk
    ndkVersion = "25.2.9519653" // Optional: Ensure it's a valid NDK version for your app

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17" 
    }

    defaultConfig {
        applicationId = "com.example.pet_care_finder"
        minSdk = 21 // Minimum Android version to support
        targetSdk = 34 // Target the latest Android SDK
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug // Use proper signing configs for production
        }
    }
}

dependencies {
    implementation "androidx.core:core-ktx:1.10.1" // Ensure core dependency for FileProvider
}
flutter {
    source = "../.."
}
